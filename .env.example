# Postgres container + DB connection
POSTGRES_USER=recipera
POSTGRES_PASSWORD=change-me
POSTGRES_DB=recipera
POSTGRES_HOST=localhost   # app on host -> localhost; app inside compose -> db
POSTGRES_PORT=5432
